# Resend API key for contact form email delivery
# Get your free key at https://resend.com
# Without this key, the contact form will accept submissions but won't send emails.
RESEND_API_KEY=

# Email address to receive project inquiry notifications
CONTACT_EMAIL=kiruthigas2208@gmail.com

# AI API key for the Kiri AI chatbot (optional)
# Uses OpenAI-compatible API. Without this, the chatbot uses a built-in FAQ system.
AI_API_KEY=
